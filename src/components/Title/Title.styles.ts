import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    fontSize: 16,
    fontWeight: "bold",
    color: "#fff",
    marginBottom: 10,
    marginTop: 20,
    marginHorizontal: 20,
  },
});

export default styles;
