import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    marginTop: 10,
    fontWeight: "bold",
    color: "white",
    fontSize: 16,
  },
});

export default styles;
