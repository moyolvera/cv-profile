import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    alignItems: "center",
    marginTop: 10,
  },
  image: {
    width: 80,
    height: 80,
  },
  text: {
    marginTop: 10,
    fontSize: 12,
    color: "white",
  },
});

export default styles;
