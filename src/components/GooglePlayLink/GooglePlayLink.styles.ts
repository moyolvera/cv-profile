import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    alignSelf: "center",
  },
  image: {
    width: 200,
    height: 140,
    resizeMode: "contain",
  },
});

export default styles;
