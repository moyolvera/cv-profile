import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    fontSize: 14,
    color: "#32313E",
    textAlign: "justify",
    fontWeight: "300",
  },
});

export default styles;
